# Aviso: Testes Movidos

## ⚠️ ATENÇÃO: Esta pasta está depreciada

Os testes foram movidos para o diretório `__tests__` na raiz do projeto.

Por favor, utilize o diretório `__tests__` para todos os novos testes e execute-os com:

```bash
pytest __tests__/
```

Esta pasta será removida em versões futuras do projeto.
